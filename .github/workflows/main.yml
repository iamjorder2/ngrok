name: Avica OCR Fixed Final

on:
  workflow_dispatch:

jobs:
  run-ocr:
    runs-on: windows-latest

    steps:
      - name: Install ngrok via NPM
        shell: pwsh
        run: |
          # npm দিয়ে ngrok ইনস্টল করা সবচেয়ে নিরাপদ পদ্ধতি
          npm install -g ngrok
          Write-Host "ngrok installed successfully via npm!"

      - name: Configure and Start ngrok
        shell: pwsh
        run: |
          # আপনার টোকেন সেট করা (Secrets থেকে আসবে)
          $token = "${{ secrets.NGROK_AUTH_TOKEN }}"
          
          # সরাসরি পোর্ট ৩৩৮৯ (RDP) এর জন্য টানেল শুরু করা
          # এখানে সরাসরি উইন্ডোজ কমান্ডলাইনে ngrok ব্যবহার করা হয়েছে
          Start-Process npx -ArgumentList "ngrok tcp 3389 --authtoken $token"
          
          Write-Host "Waiting for ngrok to generate URL..."
          Start-Sleep -Seconds 20
          
          # API থেকে URL বের করা
          try {
              $response = Invoke-RestMethod -Uri "http://localhost:4040/api/tunnels"
              $publicUrl = $response.tunnels[0].public_url
              echo "NGROK_URL=$publicUrl" >> $env:GITHUB_ENV
              Write-Host "Your Connection URL: $publicUrl"
          } catch {
              Write-Error "NGROK API did not respond. Checking if npx is running..."
              exit 1
          }

      - name: Download and Start Avica
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri "https://download.avica.com/AvicaLite_v8.0.8.9.exe" -OutFile "AvicaLite.exe"
          Start-Process -FilePath ".\AvicaLite.exe"

      - name: Capture Screen and Run OCR
        shell: pwsh
        run: |
          Start-Sleep -Seconds 30
          # স্ক্রিনশট নেওয়া
          Add-Type -AssemblyName System.Windows.Forms, System.Drawing
          $Screen = [System.Windows.Forms.Screen]::PrimaryScreen
          $Bitmap = New-Object System.Drawing.Bitmap($Screen.Bounds.Width, $Screen.Bounds.Height)
          $Graphics = [System.Drawing.Graphics]::FromImage($Bitmap)
          $Graphics.CopyFromScreen($Screen.Bounds.X, $Screen.Bounds.Y, 0, 0, $Bitmap.Size)
          $Bitmap.Save("$env:USERPROFILE\Desktop\pic.png", [System.Drawing.Imaging.ImageFormat]::Png)
          
          # পাইথন OCR রান করা
          Invoke-WebRequest -Uri "https://gitlab.com/MR.English2008/avica/-/raw/main/OCR_Avica.py" -OutFile "screenshot.py"
          python screenshot.py

      - name: Show Final URL
        shell: pwsh
        run: |
          Write-Host "------------------------------------------"
          Write-Host "CONNECT VIA: ${{ env.NGROK_URL }}"
          Write-Host "Check OCR logs for Avica ID/Pass"
          Write-Host "------------------------------------------"
          # ৬ ঘণ্টা চালু রাখা
          Start-Sleep -Seconds 21600
